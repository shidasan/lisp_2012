#include "lisp.h"
const char *bootstrap_functions[] = {
	"(defun succ (a) (+ a 1))",
	"(defun caar (a) (car (car a))",
	"(defun cadr (a) (car (cdr a))",
	"(defun cdar (a) (cdr (car a))",
	"(defun cddr (a) (cdr (cdr a))",
	"(defun caaar (a) (car (caar a))",
	"(defun caadr (a) (car (cadr a))",
	"(defun cadar (a) (car (cdar a))",
	"(defun caddr (a) (car (cddr a))",
	"(defun cdaar (a) (cdr (caar a))",
	"(defun cdadr (a) (cdr (cadr a))",
	"(defun cddar (a) (cdr (cdar a))",
	"(defun cdddr (a) (cdr (cddr a))",
	"(defun caaaar (a) (car (caaar a))",
	"(defun caaadr (a) (car (caadr a))",
	"(defun caadar (a) (car (cadar a))",
	"(defun caaddr (a) (car (caddr a))",
	"(defun cadaar (a) (car (cdaar a))",
	"(defun cadadr (a) (car (cdadr a))",
	"(defun caddar (a) (car (cddar a))",
	"(defun cadddr (a) (car (cdddr a))",
	"(defun cdaaar (a) (cdr (caaar a))",
	"(defun cdaadr (a) (cdr (caadr a))",
	"(defun cdadar (a) (cdr (cadar a))",
	"(defun cdaddr (a) (cdr (caddr a))",
	"(defun cddaar (a) (cdr (cdaar a))",
	"(defun cddadr (a) (cdr (cdadr a))",
	"(defun cdddar (a) (cdr (cddar a))",
	"(defun cddddr (a) (cdr (cdddr a))",
	"(defun append (a b) (if (null a) b (cons (car a) (append (cdr a) b))))",
	NULL
};
